#pragma once

// Disable warnings when including stb_image.h.
#pragma warning(push, 0)

// Include implementation with this include (should only be included in 1 file).
#define STB_IMAGE_IMPLEMENTATION

#include "rtk/stb/stb_image.h"

#pragma warning(pop)
